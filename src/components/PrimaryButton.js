const PrimaryButton = ({ message }) => (
    <button className='px-3 py-2 bg-primary text-white rounded-3xl '>
        <a href="">
            {message}
        </a>
    </button>
);

export default PrimaryButton;